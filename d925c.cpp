#include<stdio.h>
#include<stdlib.h>
int main(){
    int x,y,t,a,b,k,i;
    double ans=0;
    while(scanf("%d %d %d",&x,&y,&t)!=EOF)
    {
                    for(i=1;i<=t;i++)
                    {
                                     scanf("%d %d %d",&a,&b,&k);
                                     if(b<y&&b>=1)
                    }
                    printf("%.2lf\n",ans/(x*y));
                    ans=0;
    }
system("pause");
return 0;
}
